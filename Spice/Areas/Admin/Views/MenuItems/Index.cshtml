@model IEnumerable<MenuItems>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<br />
<br />
<div class="border backgroundWhite">
    <div class="row">
        <div class=" col-6">
            <h2 class="text-info">MenuItems  List</h2>
        </div>
        <div class="col-6" style="text-align:right">
            <partial name="_CreateButtonPartial" />
        </div>
    </div>
    <br />
    <div>
        @if (Model.Count() > 0)
        {
            <table class=" table table-striped border">
                <tr>
                    <th style="padding-left:30px">
                        @Html.DisplayNameFor(m=>m.Name)
                    </th>
                    <th style="padding-left:30px">
                        @Html.DisplayNameFor(m=>m.Price)

                    </th>
                    <th style="padding-left:30px">
                        @Html.DisplayNameFor(m=>m.CategoryId)
                    </th>
                     <th style="padding-left:30px">
                        @Html.DisplayNameFor(m=>m.SubCategoryId)
                    </th>
                    <th></th>
                    <th></th>
                </tr>
                @foreach (var item in Model)
                {
                    <tr>
                        <td style="padding-left:30px;">
                            @Html.DisplayFor(m=>item.Name)
                        </td>
                         <td style="padding-left:30px;">
                            @Html.DisplayFor(m=>item.Price)

                        </td>
                        <td style="padding-left:30px;">
                            @Html.DisplayFor(m=>item.Category.Name)
                        </td>
                        <td style="padding-left:30px;">
                            @Html.DisplayFor(m=>item.SubCategory.Name)
                        </td>
                        <td>
                            <partial name="_TableButtonPartial" model="item.Id" />
                        </td>

                    </tr>
                }
            </table>
        }
        else
        {
            <p style="text-align:center; color:black; font-size:25px;">No MenuItems Available....</p>
        }
    </div>
</div>

